{
  "scripts": {
    "tw:watch": "tailwindcss -i tailwind.in.css -o ./src/tailwind.out.css --watch",
    "prepare": "husky install",
    "build": "zip -r dist/hisotry-trimer.zip assets lib pages src manifest.json"
  },
  "lint-staged": {
    "**/*": "prettier --write --ignore-unknown"
  },
  "devDependencies": {
    "husky": "^8.0.3",
    "lint-staged": "^13.2.2",
    "prettier": "2.8.8",
    "tailwindcss": "^3.3.2"
  }
}
